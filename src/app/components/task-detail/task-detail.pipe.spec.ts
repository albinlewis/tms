import { TaskDetailPipe } from './task-detail.pipe';

describe('TaskDetailPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskDetailPipe();
    expect(pipe).toBeTruthy();
  });
});
